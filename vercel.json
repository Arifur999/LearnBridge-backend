{
  "version": 2,
  "buildCommand": "npx prisma generate && npx esbuild api/_handler.ts --bundle --packages=external --platform=node --target=node18 --format=esm --outfile=api/index.js",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/index"
    }
  ]
}
