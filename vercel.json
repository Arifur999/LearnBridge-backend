{
  "version": 2,
  "buildCommand": "npx prisma generate && npx esbuild api/index.ts --bundle --packages=external --platform=node --target=node18 --format=esm --outfile=dist/api/index.js",
  "builds": [
    {
      "src": "dist/api/index.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": [
          "src/templates/**"
        ]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/api/index.js"
    }
  ]
}
